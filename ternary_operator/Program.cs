namespace ternary_operator
{
    internal class Program
    {
        static void Main(string[] args)
        {
            // Ternary operator
            bool condition = true;
            string res = (condition) ? "The condition was true" : "The condition was false";
            Console.WriteLine(res);
        }
    }
}
